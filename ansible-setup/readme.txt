1) Run main-menu.sh as sudo
2) User should have an account with sudo privileges on deployed node
3) The file node.txt can have either ip addresses or hostnames (if resolveable), but ip addresses are preferred.
